"""grepsense — two-modal code search (lexical + semantic) for AI coding agents.

Exposes ``code_search`` (Zoekt trigram/regex) and ``semantic_code_search``
(embeddings) to MCP-capable agents over stdio or streamable-HTTP.
"""

__version__ = "0.2.1"
